services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: unword
      POSTGRES_PASSWORD: unword
      POSTGRES_DB: unword
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
  api:
    build:
      context: .
    ports:
      - "8080:8080"
    env_file:
      - .env.docker
    depends_on:
      - postgres

volumes:
  postgres_data: